name: "Deploy Frappe"
description: "Deploy Frappe code to a server"
author: "rtCamp"
outputs:
  out:
    description: output
runs:
  using: "docker"
  #image: "Dockerfile"
  image: 'docker://ghcr.io/rtcamp/action-deploy-frappe:v0.0.3'

branding:
  icon: "upload-cloud"
  color: "yellow"
